# DESIGN.md

## 产品定位
选题雷达 - 面向内容创作者的热点选题发现工具。精致SaaS风格，深色/浅色双主题，融入雷达扫描视觉概念。

## 气质与意象
精密的数字仪表盘，融合玻璃态质感与微妙的光影层次。深色模式如深夜的指挥中心，浅色模式如清晨的工作台。每个元素都有呼吸感，每次交互都有反馈。

## Design Tokens

### 色彩 - 深色模式
- 主背景：`#0B0F1A` - 有温度的深蓝黑
- 卡片背景：`rgba(22, 27, 45, 0.8)` + backdrop-blur(16px) - 玻璃态
- 主强调色：`#00C6ED` - 降低饱和度的青蓝
- 强调色发光：`rgba(0, 198, 237, 0.12)` - 微光晕染
- 主文字：`#F1F5F9` - 柔和白
- 次文字：`#94A3B8` - 中灰
- 辅助文字：`#64748B` - 深灰
- 边框色：`rgba(148, 163, 184, 0.08)` - 极淡边框
- 卡片边框：`rgba(148, 163, 184, 0.1)` - 微妙边框
- 卡片边框 hover：`rgba(0, 198, 237, 0.25)` - 交互反馈
- 错误色：`#F43F5E` - 玫瑰红
- 成功色：`#10B981` - 翡翠绿
- 警告色：`#F59E0B` - 琥珀

### 色彩 - 浅色模式
- 主背景：`#F8FAFC` - 微灰白
- 卡片背景：`#FFFFFF` + 细微渐变 + box-shadow
- 卡片阴影：`0 1px 3px rgba(0,0,0,0.05), 0 1px 2px rgba(0,0,0,0.03)`
- 卡片阴影 hover：`0 10px 25px rgba(0,0,0,0.08), 0 4px 10px rgba(0,0,0,0.04)`
- 边框色：`rgba(0, 0, 0, 0.06)` - 极淡
- 主文字：`#0F172A` - 近黑
- 次文字：`#475569` - 中灰
- 辅助文字：`#94A3B8` - 浅灰

### 字体
- 字体族：Inter, PingFang SC, system-ui, sans-serif
- 页面标题：20px, font-weight 700, letter-spacing -0.02em
- 区块标题：16px, font-weight 600
- 卡片标题：15px, font-weight 600
- 正文：14px, font-weight 400, line-height 1.6
- 辅助文字：12px, line-height 1.5

### 间距与圆角
- 页面最大宽度：1100px
- 卡片圆角：14px
- 按钮圆角：10px
- 标签圆角：999px (胶囊)
- 卡片内边距：20px
- 卡片间距：12px
- 区块间距：32px

### 阴影与发光
- 深色卡片：`0 4px 24px rgba(0, 0, 0, 0.2), 0 0 0 1px rgba(148, 163, 184, 0.05)`
- 深色卡片 hover：`0 8px 32px rgba(0, 0, 0, 0.3), 0 0 0 1px rgba(0, 198, 237, 0.15)`
- 浅色卡片：`0 1px 3px rgba(0,0,0,0.05), 0 1px 2px rgba(0,0,0,0.03)`
- 浅色卡片 hover：`0 10px 25px rgba(0,0,0,0.08), 0 4px 10px rgba(0,0,0,0.04)`
- 按钮 CTA：`0 2px 8px rgba(0, 198, 237, 0.25)`
- 按钮 CTA hover：`0 4px 16px rgba(0, 198, 237, 0.35)`

### 趋势标签色
- 暴涨：`rgba(244, 63, 94, 0.1)` bg + `#F43F5E` text + `rgba(244, 63, 94, 0.2)` border
- 潜力黑马：`rgba(139, 92, 246, 0.1)` bg + `#8B5CF6` text + `rgba(139, 92, 246, 0.2)` border
- 降温：`rgba(59, 130, 246, 0.1)` bg + `#3B82F6` text + `rgba(59, 130, 246, 0.2)` border
- 平稳：`rgba(148, 163, 184, 0.1)` bg + `#94A3B8` text + `rgba(148, 163, 184, 0.2)` border

## 交互与动效
- 卡片进入：stagger fade-in，每张延迟 50ms，`opacity 0→1 + translateY(8px→0)`，`duration 300ms, ease-out`
- 按钮 hover：`scale(1.02)` + 阴影增强，`transition 200ms cubic-bezier(0.4, 0, 0.2, 1)`
- 按钮 active：`scale(0.97)`
- 卡片 hover：`translateY(-2px)` + 阴影增强 + 边框亮度提升
- 收藏按钮：点击 `scale(0.85→1.1→1)` 弹跳
- Tab 切换：内容 `fade 200ms`
- 赛道卡片悬浮：`translateY(-4px)` + 背景渐变变化
- 搜索框聚焦：边框色变化 + 外发光 `0 0 0 3px rgba(0, 198, 237, 0.1)`

## 导航栏
- 固定顶部：`position: sticky; top: 0; z-index: 40`
- 玻璃态：`backdrop-blur(16px)` + 半透明背景
- 深色：`rgba(11, 15, 26, 0.85)` + 底部细边框
- 浅色：`rgba(248, 250, 252, 0.85)` + 底部细边框

## 设计禁忌
- 不要使用纯黑 `#000000` - 用深蓝黑
- 不要使用纯白背景 - 浅色用微灰白 `#F8FAFC`
- 不要使用生硬的 1px 实线边框 - 用半透明细边框
- 不要使用过大的圆角 - 保持 14px 以内
- 不要使用过多动画 - 微交互为主，300ms 以内
